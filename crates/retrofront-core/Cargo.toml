[package]
name = "retrofront-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "retrofront_core"
crate-type = ["rlib", "cdylib", "staticlib"]

[features]
default = []
