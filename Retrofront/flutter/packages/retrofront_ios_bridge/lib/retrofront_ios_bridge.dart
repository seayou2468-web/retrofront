/// iOS-only platform bridge package for Retrofront.
///
/// The app talks to the native implementation through the
/// `retrofront/document_picker` method channel; importing this package is not
/// required because Flutter auto-registers the plugin on iOS.
library retrofront_ios_bridge;
